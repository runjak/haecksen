---
import { getCollection } from "astro:content";
import FormattedDate from "../components/FormattedDate.astro";

const events = (await getCollection("events")).sort(
  (a, b) => a.data.date.valueOf() - b.data.date.valueOf()
);
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width" />
    <title>haecksen.runjak.codes</title>
  </head>
  <body>
    <h1>haecksen.runjak.codes</h1>
    <p>Something something <a href="https://dezentrale.space/events/haecksen/">haecksen</a> 🧙‍♀️💻.</p>
    <h2>Events</h2>
    <dl>
      {
        events.map((event) => (
          <>
            <dt>
              <FormattedDate date={event.data.date} />
            </dt>
            <dd>
              <a href={`/events/${event.slug}`}>{event.data.title}</a>
            </dd>
          </>
        ))
      }
    </dl>
    <h2>Topics</h2>
    A makeshift list of topics people are interested in.
    <dl>
      <dt>Git</dt>
      <dd>Git is useful for most things software</dd>
      <dt>Python</dt>
      <dd>Several people asked for it</dd>
      <dt>Typst</dt>
      <dd>A modern typesetting system</dd>
      <dt>Building websites</dt>
      <dd>Participate in the web from the other side</dd>
      <dt>Pentesting</dt>
      <dd>
        <a href="https://overthewire.org/wargames/">OverTheWire</a>,
        <a href="https://owasp.org/www-project-juice-shop/">OWASP juice shop</a>,
        <a href="https://www.hackthebox.com/">HackTheBox</a>
      </dd>
    </dl>
    <h2>Contribute</h2>
    Code for this page is on
    <a href="https://github.com/runjak/haecksen">github.com/runjak/haecksen</a>.
  </body>
</html>
